# 连接数据库的配置(开发环境)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root



#显示的
logging:
  level:
    com.example.test.demo: trace